// @flow
/**
 * Shorcuts for console loggings
 */
export const log = message => console.log(message: string|number);

/**
 * Shorcuts for console warnings
 */
export const info = message => console.info(message: string|number);